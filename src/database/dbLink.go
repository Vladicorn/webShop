package database

/*
func SelectDBLinks() []models.Link {
	rows, _ := DB.Query("SELECT id, code, userId, image,price FROM links")
	defer rows.Close()
	links := []models.Link{}
	for rows.Next() {
		link := models.Link{}
		rows.Scan(&product.Id, &product.Title, &product.Description, &product.Image, &product.Price)
		products = append(products, product)
	}
	return links
}
*/
